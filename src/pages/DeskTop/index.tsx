import styles from './index.less'

export default function IndexPage () {

  return (
    <div>
      {APP_SELECT_KEY}
      <h1 className={styles.title}>
        Page index
      </h1>
    </div>
  )
}
